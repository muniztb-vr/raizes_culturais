# Raízes Culturais — Variáveis de Ambiente do Frontend
# Copie este arquivo para .env.local e preencha os valores para desenvolvimento local.
# O .env.local está no .gitignore e nunca será commitado.

# URL base da API backend.
# Deixe comentado (ou remova) para usar o backend do Render em produção.
# Descomente e ajuste para rodar localmente:
# VITE_API_URL=http://localhost:8080/api
